datasource db {
  provider = "postgresql"
  url = "***"
}

generator client {
  provider = "prisma-client-js"
}

model Club {
  id        Int      @default(autoincrement()) @id
  createdAt DateTime @default(now())
  title     String
  rating    Int
}