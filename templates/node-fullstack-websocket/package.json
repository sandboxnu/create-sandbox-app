{
  "name": "node-fullstack-websocket",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "yarn workspace api-client build && concurrently -n \"server,app,api-client\" \"yarn workspace server dev\" \"yarn workspace app dev\" \"yarn workspace api-client dev\"",
    "build": "yarn workspace server build && yarn workspace app build",
    "start": "NODE_ENV=production node dist/index.js",
    "dev:db:up": "docker-compose -f infrastructure/dev/docker-compose.yml up",
    "dev:db:down": "docker-compose -f infrastructure/dev/docker-compose.yml down"
  },
  "workspaces": {
    "packages": [
      "app",
      "server",
      "api-client"
    ]
  },
  "devDependencies": {
    "concurrently": "^5.2.0"
  }
}
